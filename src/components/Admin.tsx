import { useEffect, useRef, useState } from 'react';
import QRCode from 'qrcode';
import { Download, Plus, RefreshCw, ShieldAlert, Trash2 } from 'lucide-react';
import { supabase } from '../lib/supabase';
import { isAdminUser, ADMIN_EMAIL } from '../lib/access';
import { formatEnrollmentCode, getDeviceRequest, type DeviceRequest } from '../lib/device';
import { useAuth } from '../hooks/useAuth';
import { useCrypto } from '../hooks/useCrypto';
import { getErrorMessage } from './ui';
import { createEncryptedMigrationExport, getEnvelopeMigrationCoverage, getPendingEnvelopeMigration, loadMigrationSourceSnapshot, prepareEnvelopeMigration, runEnvelopeMigration, activateEnvelopeMigration, type EnvelopeMigrationCoverage } from '../lib/vault-migration';

export function ApprovalRequest({ requestId, fingerprint }: { requestId: string; fingerprint: string }) {
    const { approveDeviceRequest } = useCrypto();
    const [request, setRequest] = useState<DeviceRequest | null>(null), [code, setCode] = useState(''), [expectedCode, setExpectedCode] = useState(''), [qr, setQr] = useState(''), [error, setError] = useState(''), [busy, setBusy] = useState(false);
    useEffect(() => { let live = true; void (async () => { try { const next = await getDeviceRequest(requestId); if (next.enrollmentFingerprint !== fingerprint) throw new Error('This approval link does not match the pending device request.'); const nextCode = await formatEnrollmentCode(next.enrollmentFingerprint); const url = `https://quotes.darkmg1.dev/#approve?request=${requestId}&fingerprint=${encodeURIComponent(next.enrollmentFingerprint)}`; const image = await QRCode.toDataURL(url, { margin: 1, width: 240 }); if (live) { setRequest(next); setExpectedCode(nextCode); setQr(image); } } catch (cause) { if (live) setError(cause instanceof Error ? cause.message : 'Could not load the device request.'); } })(); return () => { live = false; }; }, [fingerprint, requestId]);
    const approve = async () => { if (!request) return; setBusy(true); setError(''); try { await approveDeviceRequest(requestId, fingerprint, code); setError('Device approved. The requesting device can now unlock.'); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Could not approve this device.'); } finally { setBusy(false); } };
    return <div className="px-4 py-8 space-y-5"><h2 className="text-2xl font-bold text-white">Approve device</h2>{error && <p role="alert" className="text-sm text-red-300">{error}</p>}{request && <><p className="text-slate-300">{request.label} · {request.protectionMode === 'passkey-prf' ? 'Passkey PRF' : 'Remembered browser'}</p><p className="text-sm text-slate-400">Compare this code with the requesting device before approving.</p><p className="font-mono text-2xl tracking-widest text-white">{expectedCode}</p>{qr && <img src={qr} alt="Requesting device approval QR code" className="bg-white p-2 rounded-xl" />}<label className="block text-sm text-slate-300">Verification code<input aria-label="Verification code" value={code} onChange={event => setCode(event.target.value)} autoComplete="off" className="mt-1 w-full rounded-xl bg-slate-900 border border-slate-700 p-3 text-white" /></label><button type="button" disabled={busy || code.trim().toUpperCase() !== expectedCode} onClick={() => void approve()} className="w-full bg-primary-600 disabled:opacity-50 py-3 rounded-xl">{busy ? 'Approving…' : 'Approve device'}</button></>}</div>;
}

export const AdminDashboard = () => {
    const { user } = useAuth(); const { deviceId, getDeviceAuthorization } = useCrypto(); const isAdmin = isAdminUser(user); const [allowlist, setAllowlist] = useState<{ id: string; email: string }[]>([]); const [email, setEmail] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState('');
    const authorization = async () => deviceId ? await getDeviceAuthorization() : null;
    const refresh = async () => { setBusy(true); setError(''); try { const auth = await authorization(); const { data, error } = await supabase.rpc('list_members', { p_device_id: auth?.deviceId ?? null, p_device_token: auth?.token ?? null }); if (error) throw error; if (data === null || !Array.isArray(data)) throw new Error('Device authorization was denied. Unlock this device and retry.'); setAllowlist(data.filter((item): item is { id: string; email: string } => !!item && typeof item === 'object' && typeof item.id === 'string' && typeof item.email === 'string')); } catch (cause) { setError(getErrorMessage(cause, 'Could not load allowed emails.')); } finally { setBusy(false); } };
    // eslint-disable-next-line react-hooks/set-state-in-effect, react-hooks/exhaustive-deps
    useEffect(() => { if (isAdmin) void refresh(); }, [isAdmin]);
    const add = async (event: React.FormEvent) => { event.preventDefault(); if (!email.trim()) return; setBusy(true); setError(''); try { const auth = await authorization(); const { data, error } = await supabase.rpc('add_member', { p_email: email.trim().toLowerCase(), p_device_id: auth?.deviceId ?? null, p_device_token: auth?.token ?? null }); if (error) throw error; if (data === null || typeof data !== 'object' || typeof data.email !== 'string') throw new Error('Device authorization was denied. Unlock this device and retry.'); setEmail(''); await refresh(); } catch (cause) { setError(getErrorMessage(cause, 'Could not add email.')); } finally { setBusy(false); } };
    const remove = async (item: { id: string; email: string }) => { setBusy(true); setError(''); try { const auth = await authorization(); const { data, error } = await supabase.rpc('remove_member_access', { p_member_id: item.id, p_device_id: auth?.deviceId ?? null, p_device_token: auth?.token ?? null }); if (error) throw error; if (data === null || typeof data !== 'object' || data.status !== 'removed' || typeof data.id !== 'string') throw new Error('Device authorization was denied. Unlock this device and retry.'); const ownerId = data.owner_id; if (ownerId !== null && typeof ownerId !== 'string') { setError('Member access was removed, but their active session could not be invalidated.'); await refresh(); return; } if (ownerId) { const invalidation = await supabase.functions.invoke('vault-security', { body: { action: 'invalidate_member_session', ownerId } }); if (invalidation.error || !invalidation.data || invalidation.data.ownerId !== ownerId || invalidation.data.status !== 'invalidated') { setError('Member access was removed, but their active session could not be invalidated.'); await refresh(); return; } } await refresh(); } catch (cause) { setError(getErrorMessage(cause, 'Could not remove email.')); } finally { setBusy(false); } };
    if (!isAdmin) return <div className="p-8 text-center"><ShieldAlert className="w-12 h-12 text-red-400 mx-auto" /><h2 className="mt-4 text-2xl font-bold">Access denied</h2></div>;
    return <div className="px-4 py-6 space-y-6"><div className="flex justify-between"><div><h2 className="text-2xl font-bold text-white">Access control</h2><p className="text-sm text-slate-400">Manage allowed sign-up emails and approve devices from their secure link.</p></div><button aria-label="Refresh allowlist" onClick={() => void refresh()}><RefreshCw className={busy ? 'animate-spin' : ''} /></button></div>{error && <p role="alert" className="text-red-300">{error}</p>}<form onSubmit={add} className="flex gap-2"><input aria-label="Email address to allow" type="email" required value={email} onChange={event => setEmail(event.target.value)} className="flex-1 rounded-xl bg-slate-900 border border-slate-700 p-3 text-white" /><button aria-label="Add email to allowlist" disabled={busy} className="rounded-xl bg-primary-600 px-5"><Plus /></button></form><ul className="divide-y divide-slate-700">{allowlist.map(item => <li key={item.id} className="py-3 flex justify-between"><span>{item.email}</span><button aria-label={`Remove ${item.email} from allowlist`} disabled={busy || item.email.trim().toLowerCase() === ADMIN_EMAIL} onClick={() => void remove(item)}><Trash2 className="w-4 h-4 text-red-400" /></button></li>)}</ul><MigrationPanel /></div>;
};

function MigrationPanel() {
    const { user, canSync } = useAuth();
    const cryptoContext = useCrypto() as ReturnType<typeof useCrypto> & { envelopeStatus?: string | null; getApprovedTargetMasterKey?: (generation: string) => Promise<Uint8Array>; setPreparedTargetMasterKey?: (generation: string, key: Uint8Array) => void; refreshVaultState?: () => Promise<void> };
    const { encryptionKey, vaultGeneration, legacyVaultGeneration, deviceId, getDeviceAuthorization } = cryptoContext;
    const sourceGeneration = legacyVaultGeneration ?? vaultGeneration;
    const [exported, setExported] = useState(false), [snapshot, setSnapshot] = useState<{ revision: number; quotes: import('../types').Quote[] } | null>(null), [pending, setPending] = useState<Awaited<ReturnType<typeof getPendingEnvelopeMigration>> | null>(null), [coverage, setCoverage] = useState<EnvelopeMigrationCoverage | null>(null), [confirmActivation, setConfirmActivation] = useState(false);
    const targetKey = useRef<Uint8Array | null>(null);
    const [busy, setBusy] = useState(false), [message, setMessage] = useState(''), [error, setError] = useState('');
    const visible = cryptoContext.envelopeStatus === 'legacy' || cryptoContext.envelopeStatus === 'preparing';
    useEffect(() => { if (!visible || !sourceGeneration) return; void (async () => { try { const auth = deviceId ? await getDeviceAuthorization() : null; const next = await getPendingEnvelopeMigration(auth?.deviceId ?? null, auth?.token ?? null); setPending(next); if (next.migrationId && auth) setCoverage(await getEnvelopeMigrationCoverage(next.migrationId, auth.deviceId, auth.token)); } catch (cause) { setError(getErrorMessage(cause, 'Could not load migration status.')); } })(); return () => { targetKey.current?.fill(0); targetKey.current = null; }; }, [deviceId, getDeviceAuthorization, sourceGeneration, visible]);
    if (!visible || !sourceGeneration || !encryptionKey || !user) return null;
    const downloadBackup = async () => { setBusy(true); setError(''); try { const auth = deviceId ? await getDeviceAuthorization() : null; const next = await loadMigrationSourceSnapshot({ sourceGeneration, deviceId: auth?.deviceId, token: auth?.token }); const file = await createEncryptedMigrationExport({ generation: sourceGeneration, revision: next.revision, quotes: next.quotes, key: encryptionKey }); const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([file], { type: 'application/json' })); link.download = `quotevault-encrypted-migration-${new Date().toISOString().slice(0, 10)}.json`; link.click(); URL.revokeObjectURL(link.href); setSnapshot(next); setExported(true); setMessage('Encrypted backup downloaded. Keep it until activation is verified.'); } catch (cause) { setError(getErrorMessage(cause, 'Could not create the encrypted backup.')); } finally { setBusy(false); } };
    const prepare = async () => { if (!snapshot) return; setBusy(true); setError(''); try { const key = crypto.getRandomValues(new Uint8Array(32)); targetKey.current = key; const result = await prepareEnvelopeMigration({ sourceGeneration, sourceRevision: snapshot.revision, sourceKey: encryptionKey, sourceQuotes: snapshot.quotes, targetMasterKey: key, actorId: user.id, encryptedExportConfirmed: exported }); cryptoContext.setPreparedTargetMasterKey?.(result.targetGeneration, key); await cryptoContext.refreshVaultState?.(); setPending({ migrationId: result.migrationId, status: 'prepared', sourceGeneration, targetGeneration: result.targetGeneration, sourceRevision: result.sourceRevision, expectedQuoteCount: result.quoteCount, stagedQuoteCount: 0 }); if (deviceId) { const auth = await getDeviceAuthorization(); setCoverage(await getEnvelopeMigrationCoverage(result.migrationId, auth.deviceId, auth.token)); } setMessage('Migration prepared. Complete target device approval before staging.'); } catch (cause) { targetKey.current?.fill(0); targetKey.current = null; setError(getErrorMessage(cause, 'Migration preparation failed.')); } finally { setBusy(false); } };
    const stage = async () => { if (!snapshot || !pending?.migrationId || !deviceId) return; setBusy(true); setError(''); try { const auth = await getDeviceAuthorization(); const targetGeneration = pending.targetGeneration; if (!targetGeneration) throw new Error('Migration target generation is not available yet.'); const key = targetKey.current ?? (cryptoContext.getApprovedTargetMasterKey ? await cryptoContext.getApprovedTargetMasterKey(targetGeneration) : null); if (!key) throw new Error('An approved target device is required before staging.'); targetKey.current = key; const devices = coverage?.members.flatMap(member => member.devices.filter(device => !device.wrapperStaged).map(device => ({ id: device.deviceId, publicJwk: device.publicJwk, publicKeyFingerprint: device.publicKeyFingerprint }))) ?? []; const recoveries = coverage?.members.flatMap(member => member.recoveryKeys.filter(keyItem => !keyItem.wrapperStaged).map(keyItem => ({ id: keyItem.recoveryKeyId, publicJwk: keyItem.publicJwk, publicKeyFingerprint: keyItem.publicKeyFingerprint }))) ?? []; const result = await runEnvelopeMigration({ sourceGeneration, sourceRevision: snapshot.revision, sourceQuotes: snapshot.quotes, sourceKey: encryptionKey, targetGeneration, targetMasterKey: key, migrationId: pending.migrationId, deviceId: auth.deviceId, token: auth.token, actorId: user.id, deviceWrappers: devices, recoveryWrappers: recoveries, encryptedExportConfirmed: exported, onProgress: next => setMessage(`${next.state}: ${next.stagedQuoteCount}/${next.expectedQuoteCount} staged`) }); setMessage(`Migration ${result.status}. Review device coverage before activation.`); setCoverage(await getEnvelopeMigrationCoverage(result.migrationId, auth.deviceId, auth.token)); } catch (cause) { targetKey.current?.fill(0); targetKey.current = null; setError(getErrorMessage(cause, 'Migration staging failed.')); } finally { setBusy(false); } };
    const activate = async () => { if (!pending?.migrationId || !snapshot || !deviceId) return; if (!confirmActivation || !coverage || !coverage.ready || coverage.stagedQuoteCount !== coverage.expectedQuoteCount || coverage.members.some(member => member.blockers.length > 0)) { setError('Resolve every device blocker and confirm readiness before activation.'); return; } setBusy(true); setError(''); try { const auth = await getDeviceAuthorization(); const targetGeneration = pending.targetGeneration; if (!targetGeneration) throw new Error('Migration target generation is not available yet.'); const key = targetKey.current ?? (cryptoContext.getApprovedTargetMasterKey ? await cryptoContext.getApprovedTargetMasterKey(targetGeneration) : null); if (!key) throw new Error('The approved target device key is unavailable.'); targetKey.current = key; const result = await activateEnvelopeMigration({ sourceGeneration, sourceRevision: snapshot.revision, sourceQuotes: snapshot.quotes, sourceKey: encryptionKey, targetGeneration, targetMasterKey: key, migrationId: pending.migrationId, deviceId: auth.deviceId, token: auth.token, actorId: user.id, encryptedExportConfirmed: exported, onProgress: next => setMessage(`${next.state}: ${next.verifiedQuoteCount}/${next.expectedQuoteCount} verified`) }); await cryptoContext.refreshVaultState?.(); setMessage(`Migration finalized at generation ${result.targetGeneration}.`); targetKey.current.fill(0); targetKey.current = null; setPending(null); } catch (cause) { targetKey.current?.fill(0); targetKey.current = null; setError(getErrorMessage(cause, 'Migration activation failed.')); } finally { setBusy(false); } };
    const refreshCoverage = async () => { if (!pending?.migrationId || !deviceId) return; setBusy(true); try { const auth = await getDeviceAuthorization(); setCoverage(await getEnvelopeMigrationCoverage(pending.migrationId, auth.deviceId, auth.token)); } catch (cause) { setError(getErrorMessage(cause, 'Could not refresh migration readiness.')); } finally { setBusy(false); } };
    return <section className="rounded-2xl border border-amber-500/30 bg-amber-500/5 p-5 space-y-4" aria-labelledby="migration-heading"><div><h3 id="migration-heading" className="text-lg font-semibold text-white">Encrypted vault migration</h3><p className="mt-1 text-sm text-slate-300">Download an encrypted snapshot, prepare secure devices, stage the exact snapshot in batches, review coverage, then activate explicitly.</p></div><p className="text-sm text-amber-200">Keep the encrypted backup until verification completes. It contains ciphertext only; never upload it or share the vault key.</p>{error && <p role="alert" className="text-sm text-red-300">{error}</p>}{message && <p role="status" className="text-sm text-slate-200">{message}</p>}<div className="flex flex-wrap gap-3"><button type="button" onClick={() => void downloadBackup()} disabled={busy || !canSync} className="inline-flex items-center gap-2 rounded-xl border border-slate-600 px-4 py-2 text-sm disabled:opacity-50"><Download className="h-4 w-4" />Download encrypted backup</button><button type="button" onClick={() => void prepare()} disabled={busy || !exported || !snapshot || !!pending?.migrationId} className="rounded-xl bg-slate-700 px-4 py-2 text-sm font-medium disabled:opacity-50">Prepare secure devices</button><button type="button" onClick={() => void refreshCoverage()} disabled={busy || !pending?.migrationId || !deviceId} className="rounded-xl border border-slate-600 px-4 py-2 text-sm disabled:opacity-50">Refresh readiness</button><button type="button" onClick={() => void stage()} disabled={busy || !exported || !snapshot || !pending?.migrationId || !deviceId || !coverage || coverage.members.some(member => member.blockers.length > 0)} className="rounded-xl bg-amber-600 px-4 py-2 text-sm font-medium disabled:opacity-50">Stage exact snapshot</button><button type="button" onClick={() => void activate()} disabled={busy || !pending?.migrationId || !coverage || !coverage.ready || coverage.stagedQuoteCount !== coverage.expectedQuoteCount || coverage.members.some(member => member.blockers.length > 0) || !confirmActivation} className="rounded-xl bg-red-600 px-4 py-2 text-sm font-medium disabled:opacity-50">Activate after review</button></div>{coverage && <ul className="text-xs text-slate-300 space-y-1">{coverage.members.map(member => <li key={member.memberId ?? member.email ?? 'allowlist-entry'}>{member.email ?? 'Allowlisted member'}: {member.blockers.length ? member.blockers.join(', ') : 'ready'}</li>)}</ul>}<label className="flex items-start gap-2 text-xs text-slate-400"><input type="checkbox" checked={exported} onChange={event => setExported(event.target.checked)} disabled={busy} />I downloaded and retained the encrypted backup.</label><label className="flex items-start gap-2 text-xs text-slate-400"><input type="checkbox" checked={confirmActivation} onChange={event => setConfirmActivation(event.target.checked)} disabled={busy} />I reviewed device coverage and authorize vault activation.</label></section>;
}
